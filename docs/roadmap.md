# PickDeal 남은 작업

> 갱신: 2026-09-25
> 미해결 작업과 보류 판단만 관리한다. 완료 이력은 Git, 계약은 `docs/01~06`, 조사 근거는 `docs/notes/`를 따른다. 구현 현황과 설계 규칙을 여기 반복하지 않는다.

## 우선 확인할 작업

- [ ] 쇼핑몰 별칭 통합 커밋 `91c4ea5`를 배포하고 대표 이름·과거 별칭 URL·복수 선택·더 보기를 확인한다. 로컬 구현과 테스트는 완료했으며 배포 반영은 미확인이다.
- [ ] 개드립 이미지 표시 문제의 처리 방식을 결정한다. 2026-09-25 배포 글 `/deals/391`의 HTML에 썸네일 URL은 있었지만 브라우저 표시가 실패했고, 해당 URL의 HEAD 요청(Referer: 배포 사이트)은 403이었다. 외부 이미지 제한인지 봇 방어인지는 미확정이며 차단 우회는 하지 않는다.
- [ ] 재수집 시 썸네일 갱신 정책을 정한다. 현재는 신규 저장 시에만 URL을 저장한다. 유효한 새 URL은 반영하고 미확보 시 기존 값을 보존하는 방안을 검토하되, 위 403 문제와는 구분한다.
- [ ] 운영 실행 커밋·출처별 최근 수집 로그·중복 스케줄러 여부·백업 및 테스트 DB 복구 결과를 확인한다. 구성·검증 절차는 `docs/06`을 따른다.

## 필요가 확인되면 다시 판단할 것

- 알림: 상시 수집이 안정된 뒤 한 채널만 선택한다. 남은 데모 알림·AI UI의 유지 여부도 별도로 결정한다.
- AI 댓글 요약: 댓글 확보 가능성과 요청·저장 비용을 먼저 확인한다.
- 조회 성능·서버 worker 분리·Redis: 조회량·응답 시간·수집 지연 등 실제 병목을 근거로 검토한다.
- 인증·멀티유저: 공개 쓰기 기능과 사용자별 데이터 격리가 필요할 때 별도 범위를 정한다.
- 가격 이력은 제외 상태를 유지하며, 읽음·관심 없음 등 개인화는 반복되는 사용 불편이 확인될 때 검토한다.
