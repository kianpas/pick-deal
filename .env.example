# Copy to .env and set a password before running Docker Compose.
DB_NAME=pickdeal
DB_USERNAME=pickdeal
DB_PASSWORD=
BACKEND_PORT=8080
# Enable only on the single intended collector host after checking the deployment.
COLLECTOR_ENABLED=false
# Remote collector API stays disabled until explicitly configured. Never expose the token to frontend.
# When enabling, use a random 32-256 character token (letters, digits, underscore, hyphen).
COLLECTOR_INGRESS_ENABLED=false
COLLECTOR_INGRESS_TOKEN=
# Exact frontend origins, comma-separated. Empty means no cross-origin browser access.
CORS_ALLOWED_ORIGINS=https://pick-deal.vercel.app
JAVA_TOOL_OPTIONS='-Xms128m -Xmx768m -XX:+ExitOnOutOfMemoryError'
